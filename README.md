# backend

Using Python3.11

To Run Server: python3 -m venv venv

Install Flask: pip install Flask
Install Flask Packages: pip freeze | grep Flask >> requirements.txt
Install Packages: pip install -r requirements.txt

Run in Debug mode: flask --app app --debug run

Stop Server: deactivate

